

ccoommmm                                                         ccoommmm



NNAAMMEE
     comm - print lines common to two files

SSYYNNOOPPSSIISS
     ccoommmm [-112233] _f_i_l_e_1 _f_i_l_e_2

DDEESSCCRRIIPPTTIIOONN
     ccoommmm reads _f_i_l_e_1 and _f_i_l_e_2 line by line and compares them,
     printing each line in one of three columns separated by
     tabs.  Column 1 holds lines found only in _f_i_l_e_1, column 2
     holds lines found only in _f_i_l_e_2, and column 3 holds lines
     found in both.  Both files should already be sorted into the
     same order, since ccoommmm merges rather than searches them.
     Either _f_i_l_e_1 or _f_i_l_e_2 (but not both) may be - to mean the
     standard input.

     -11   Do not print column 1 (lines unique to _f_i_l_e_1).

     -22   Do not print column 2 (lines unique to _f_i_l_e_2).

     -33   Do not print column 3 (lines common to both files).

SSEEEE AALLSSOO
     ccmmpp, ssoorrtt

DDIIAAGGNNOOSSTTIICCSS
     ccoommmm exits 1 if either file cannot be opened, or if it is
     not given exactly two file arguments.
